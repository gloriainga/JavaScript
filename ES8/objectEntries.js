const objHero = {
    id: 92,
    name: "Batman",
    universe: "DC Comics"
};

const result = Object.entries(objHero);
console.log(result);

//transforma un objeto en una array de arrays y la estructura va a ser 
//propiedad valor